id: cache.reassessment.evidence
name: 缓存重估证据快照
category: system
status: beta
visibility: internal
version: 1
summary: >
  Phase 4 只读证据快照：从现有 result_meta 信号聚合 exact replay hit 率与
  intentional repeated-question 率，判断是否需要语义缓存 shadow evaluation。
  只收集证据、不 serve 任何缓存；进程内聚合、重启清零。
intent_examples:
  - 查看缓存重估证据
  - 缓存重估指标
primary_entry:
  type: mcp
  name: fin_cache_reassessment_evidence
input_schema: {}
output_schema:
  schema_version: string
  total_consultations: number
  idempotency_replay_count: number
  idempotency_replay_rate: number
  fresh_count: number
  intentional_repeat_count: number
  intentional_repeat_rate_of_fresh: number
  continuity: object
side_effect: read_only
risk_level: low
requires_confirmation: false
profile_sensitive: false
audit_fields:
- total_consultations
- idempotency_replay_count
- intentional_repeat_count
